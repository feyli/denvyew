import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Metrics",
    description: "Overview of the current machine metrics",
}

export default function Metrics() {
    return (
        <>
        There will be something, eventually
        </>
    );
}